pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
            }
        }
        agent { dockerfile true }
        stage('Execute') {
            steps {
                sh 'docker --version'
            }
        }
    }
}