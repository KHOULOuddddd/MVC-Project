pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
            }
              steps {
                sh 'docker build -t dotnet .'
            }
        
        }
       
        
          
    }
}