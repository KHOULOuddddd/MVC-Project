pipeline {
    agent none
    stages {
        stage('Build') {
            agent any
            steps {
                checkout scm
                bat 'make'
                bat 'docker build -t dotnet .' 
            }
        }
    }
}