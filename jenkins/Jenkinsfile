pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
                bat 'docker build -t dotnet .'
            }  
        }
        stage('Dotnet Test') {
        steps {
          sh 'dotnet MVC/MVC Test Project/Controllers/HomeControllerTest.cs --test'
        }
      }
    }
}