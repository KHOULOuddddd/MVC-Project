pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
                bat 'docker build -t dotnet .'
            }  
        }
        stage('Unit Tests') {
            steps {
                sh 'mvn surefire:test'
            }
        }
    }
}