pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
                bat 'docker build -t dotnet .'
            }  
        }
        stage('Dotnet Test') {
        steps {
          sh 'dotnet bin/HomeControllerTest.cs --test'
        }
      }
    }
}