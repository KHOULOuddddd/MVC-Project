pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm
            }
        }
        stage('Execute') {
            agent {
                { dockerfile true }
            }
            steps {
                sh 'docker --version'
            }
        }
    }
}